{
  "private": true,
  "name": "linters",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint .",
    "test": "lerna run test",
    "release": "yarn test && lerna publish",
    "precommit": "lint-staged",
    "commitmsg": "commitlint -e $GIT_PARAMS"
  },
  "engines": {
    "node": ">=8"
  },
  "repository": "https://github.com/priver/linters",
  "devDependencies": {
    "@commitlint/cli": "^7.0.0",
    "eslint": "^5.2.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-node": "^7.0.1",
    "eslint-plugin-react": "^7.10.0",
    "husky": "^0.14.3",
    "lerna": "^3.0.0-rc.0",
    "lint-staged": "^7.2.0"
  }
}
